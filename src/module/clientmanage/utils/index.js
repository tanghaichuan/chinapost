/** 重组树结构
  * @export
  * @param {Object} obj
  * @return {Object}
  */
export function reGroupTree(tree, obj) {
    // if (!obj.hasOwnProperty("VALUE") && _.isObject(obj) || _.isArray(obj)) {
    //     Object
    //         .keys(obj)
    //         .forEach((key, index) => {
    //             console.log(key, index);
    //         })
    // }else if()
}